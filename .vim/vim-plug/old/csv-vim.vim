" Use tabs instead of commas as the delimiter
let g:csv_delimiter = "\t"

" Enable auto-formatting of CSV files
let g:csv_autoformat = 1

" Highlight the column under the cursor
let g:csv_highlight_column = 1

" Set the maximum width for columns when auto-formatting
let g:csv_max_fieldwidth = 30
